

<h1 class="ms-5 mt-3">Perfil de {{player.name}} </h1>

{{#if player}}
<div class="d-flex ms-5 mt-3">
    <div class="me-3">
        <img width='300'src="{{player.avatar}}" alt="" referrerpolicy="no-referrer"/>
    </div>
    <div>
        <p>{{player.email}}</p>
        {{#if player.city}}<p>{{player.city}}</p>{{/if}}  
    </div>
</div>

<div class="d-flex ms-5 mt-3"><i class="me-2 bi bi-heart-fill" style="color: rgb(250, 93, 93)"></i>Salas que le gustan a {{player.name}}:</div>
<div class="container w-75 mt-5 section-body">
  {{#if likes}}
  <div class="room-card mb-4 flex-sm-row">
    {{#each likes}}
    <div class="card" style="width: 18rem;">
      <img src="{{room.image}}" class="card-img-top img-cards" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{room.name}}</h5>
        <a href="/company/{{room.company._id}}">
          <p class="card-text">{{room.company.companyName}}</p>
        </a>
        <p class="card-text">{{room.company.city}}</p>

        <a href="/room/{{room._id}}" class="btn btn-primary">ver más</a>
      </div>
    </div>
    {{/each}}
    {{else}}
    <p class="mt-4">{{player.name}} todavía no ha indicado que le guste ninguna sala.</p>
    {{/if}}
</div>
</div>

<div class="d-flex ms-5 mt-3"><i class="me-2 bi bi-check-circle-fill" style="color: rgb(114, 206, 134)"></i>Salas que {{player.name}} ha hecho:</div>
    <div class="container w-75 mt-5 section-body">
     {{#if dones}}
  <div class="room-card mb-4 flex-sm-row">
    {{#each dones}}
    <div class="card" style="width: 18rem;">
      <img src="{{room.image}}" class="card-img-top img-cards" alt="...">
      <div class="card-body">
        <h5 class="card-title">{{room.name}}</h5>
        <a href="/company/{{room.company._id}}">
          <p class="card-text">{{room.company.companyName}}</p>
        </a>
        <p class="card-text">{{room.company.city}}</p>

        <a href="/room/{{room._id}}" class="btn btn-primary">ver más</a>
      </div>
    </div>
    {{/each}}
    {{else}}
    <p class="mt-4 ms-5">{{player.name}} no ha marcado ninguna sala como hecha.</p>
{{/if}}

    

{{else}}
<p>No hay jugador</p>
{{/if}}
</div>
</div>