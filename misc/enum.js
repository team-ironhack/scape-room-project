const { model } = require("mongoose");

const PLAYERS = ['2-4', '2-5', '2-6'];
const DIFFICULTY = ['Baja', 'Media', 'Alta'];
const TERROR = ["No", "Temor", "Miedo", "Horror", "Terror", "Pánico"]

module.exports = {
    PLAYERS,
    DIFFICULTY,
    TERROR
}